#[cfg(feature = "v1")]
pub mod v1;
pub mod management;
pub mod utils;
pub mod files;
pub mod status;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
